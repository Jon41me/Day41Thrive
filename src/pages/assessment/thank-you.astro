---
import Layout from '../../layouts/Layout.astro';
import Header from '../../components/Header.astro';
import FloatingActionButton from '../../components/FloatingActionButton.astro';
---

<Layout title="Thank You - Day 41 Thrive">
    <Header />
    <main class="thank-you-page">
        <section class="thank-you-section">
            <div class="container">
                <div class="thank-you-card">
                    <h1>Thank You for Completing the Growth Gauge!</h1>
                    <p class="message">We appreciate you taking the time to share your business insights with us. A member of our team will review your responses and reach out to you shortly to discuss how we can help your business thrive.</p>
                    <div class="next-steps">
                        <h2>What's Next?</h2>
                        <ul>
                            <li>Our team will analyze your responses</li>
                            <li>We'll prepare personalized recommendations</li>
                            <li>A team member will contact you within 1-2 business days</li>
                        </ul>
                    </div>
                   <div class="hirebus-intro">
    <h2>Discover Your Leadership Style</h2>
    <p>
        Take the Hirebus Assessment to uncover your strengths, leadership style, and communication preferences. 
        This short personality survey helps us better support you and ensures our recommendations truly fit your goals.
    </p>
</div>
<div class="cta-buttons">
<a href="https://app.hirebus.com/survey/register?co=day41thrive" class="btn hirebus-btn" target="_blank" rel="noopener noreferrer">
        Take the Hirebus Assessment
    </a>
   
    
</div>

                </div>
            </div>
        </section>
    </main>
    <FloatingActionButton />
</Layout>

<style>
    .thank-you-page {
        padding-top: 80px;
    }

    .thank-you-section {
        padding: 4rem 0;
        background: rgba(255, 20, 147, 0.05);
        min-height: calc(100vh - 80px);
    }

    .container {
        max-width: 800px;
        margin: 0 auto;
        padding: 0 1.5rem;
    }

    .thank-you-card {
        background: var(--card-bg);
        padding: 2.5rem;
        border-radius: 0.5rem;
        border: 1px solid var(--primary);
        text-align: center;
    }

    h1 {
        color: var(--primary);
        font-size: clamp(1.75rem, 3vw, 2.5rem);
        margin-bottom: 1.5rem;
    }

    .message {
        font-size: clamp(1rem, 1.5vw, 1.25rem);
        line-height: 1.6;
        margin-bottom: 2rem;
        color: var(--text);
    }

    .next-steps {
        background: rgba(255, 20, 147, 0.1);
        padding: 2rem;
        border-radius: 0.5rem;
        margin-bottom: 2rem;
        text-align: left;
    }

    .next-steps h2 {
        color: var(--primary);
        font-size: clamp(1.25rem, 2vw, 1.5rem);
        margin-bottom: 1rem;
    }

    .next-steps ul {
        list-style: none;
        padding: 0;
    }

    .next-steps li {
        margin-bottom: 0.75rem;
        padding-left: 1.5rem;
        position: relative;
        color: var(--text);
    }

    .next-steps li::before {
        content: "✓";
        color: var(--primary);
        position: absolute;
        left: 0;
    }

    .cta-buttons {
        display: flex;
        gap: 1rem;
        justify-content: center;
    }

    .btn {
        padding: 0.8rem 1.5rem;
        border-radius: 0.25rem;
        text-decoration: none;
        font-weight: 600;
        transition: all 0.3s ease;
    }

    .home-btn {
        background: var(--primary);
        color: white;
    }

    .blog-btn {
        background: transparent;
        border: 2px solid var(--primary);
        color: var(--primary);
    }

    .btn:hover {
        transform: translateY(-2px);
    }

    @media (max-width: 768px) {
        .thank-you-card {
            padding: 1.5rem;
            margin: 0 1rem;
        }

        .cta-buttons {
            flex-direction: column;
        }

        .btn {
            width: 100%;
            text-align: center;
        }
    }
    .hirebus-intro {
    margin-bottom: 2rem;
    text-align: center;
}

.hirebus-intro h2 {
    color: var(--primary);
    font-size: clamp(1.25rem, 2vw, 1.5rem);
    margin-bottom: 0.75rem;
}

.hirebus-intro p {
    font-size: clamp(1rem, 1.5vw, 1.25rem);
    color: var(--text);
    line-height: 1.6;
}

</style>