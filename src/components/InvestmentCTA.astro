---
---
<div class="cta-section">
    <h2 class="animated-title">Choose Your Investment in Success</h2>
    <p class="cta-description">Your leadership journey begins with the decision to invest in your future. Whether you're building a foundation or ready for a full-scale transformation, Day 41 Thrive has a package designed to help you achieve your goals.</p>
    <div class="contact-info">
        <a href="mailto:day41thrive@gmail.com" class="contact-link">
            <span class="icon">📩</span>
            <span>day41thrive@gmail.com</span>
        </a>
        <a href="tel:877-DAY-41ME" class="contact-link">
            <span class="icon">📞</span>
            <span>877-DAY-41ME</span>
        </a>
    </div>
    <div class="founder-links">
        <a href="https://www.jonathonbroce.com" target="_blank" rel="noopener noreferrer" class="founder-link">
            Jonathon Broce - www.jonathonbroce.com
        </a>
        <a href="https://www.jeffvalder.com" target="_blank" rel="noopener noreferrer" class="founder-link">
            Jeff Valder - www.jeffvalder.com
        </a>
    </div>
    <p class="closing animated-title">Let's thrive together!</p>
</div>

<style>
    .cta-section {
        text-align: center;
        max-width: 800px;
        margin: 2rem auto;
        padding: 2.5rem;
        background: var(--card-bg);
        border-radius: 0.5rem;
        border: 1px solid var(--primary);
        transition: transform 0.3s ease;
    }
    .cta-section:hover {
        transform: translateY(-4px);
    }
    .animated-title {
        display: inline-block;
        position: relative;
        transition: transform 0.3s ease;
        text-align: center;
        width: 100%;
        margin-bottom: 2rem;
        font-size: clamp(1.5rem, 2.5vw, 2rem);
    }
    .animated-title:hover {
        transform: scale(1.05);
        color: var(--primary);
        animation: pulse 1.5s infinite;
    }
    .animated-title::after {
        content: '';
        position: absolute;
        left: 50%;
        right: 50%;
        bottom: -2px;
        height: 2px;
        background: var(--primary);
        transform: scaleX(0);
        transition: all 0.3s ease;
    }
    .animated-title:hover::after {
        left: 25%;
        right: 25%;
        transform: scaleX(1);
    }
    @keyframes pulse {
        0% {
            box-shadow: 0 0 0 0 rgba(255, 20, 147, 0.4);
        }
        70% {
            box-shadow: 0 0 0 10px rgba(255, 20, 147, 0);
        }
        100% {
            box-shadow: 0 0 0 0 rgba(255, 20, 147, 0);
        }
    }
    .cta-description {
        font-size: 1.1rem;
        color: var(--text);
        margin: 1.5rem 0;
        line-height: 1.6;
    }
    .contact-info {
        display: flex;
        flex-direction: column;
        gap: 1rem;
        margin: 2rem 0;
        align-items: center;
    }
    .contact-link {
        color: var(--primary);
        text-decoration: none;
        font-size: 1.2rem;
        transition: all 0.3s ease;
        display: flex;
        align-items: center;
        gap: 0.5rem;
        padding: 0.5rem 1rem;
        border-radius: 0.25rem;
    }
    .contact-link:hover {
        color: var(--secondary);
        transform: translateY(-2px);
    }
    .icon {
        font-size: 1.4rem;
    }
    .founder-links {
        margin-top: 2rem;
        padding-top: 2rem;
        border-top: 1px solid var(--primary);
        display: flex;
        flex-direction: column;
        gap: 1rem;
    }
    .founder-link {
        color: var(--text);
        text-decoration: none;
        font-size: 1.2rem;
        transition: color 0.3s ease;
        padding: 0.5rem;
    }
    .founder-link:hover {
        color: var(--primary);
    }
    .closing {
        margin-top: 2rem;
        font-size: 1.4rem;
        color: var(--primary);
        font-weight: bold;
        margin-bottom: 0;
    }

    @media (max-width: 768px) {
        .cta-section {
            padding: 1.5rem;
            margin: 1rem auto;
        }
        .contact-link {
            font-size: 1rem;
        }
        .founder-link {
            font-size: 1.1rem;
        }
        .closing {
            font-size: 1.2rem;
        }
    }
</style>