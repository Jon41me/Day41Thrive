---
---
<div class="blog-contact-cta">
    <h3>Ready to Transform Your Leadership?</h3>
    <p>Let's connect and discuss how Day 41 Thrive can help you and your team reach new heights.</p>
    <a href="/contact" class="btn contact-btn">Contact Us Today</a>
</div>

<style>
    .blog-contact-cta {
        margin-top: 4rem;
        padding: 2rem;
        background: var(--card-bg);
        border-radius: 0.5rem;
        border: 1px solid var(--primary);
        text-align: center;
        transition: transform 0.3s ease;
    }

    .blog-contact-cta:hover {
        transform: translateY(-4px);
    }

    h3 {
        color: var(--primary);
        font-size: 1.5rem;
        margin-bottom: 1rem;
    }

    p {
        color: var(--text);
        margin-bottom: 1.5rem;
        font-size: 1.1rem;
        line-height: 1.6;
    }

    .contact-btn {
        display: inline-block;
        padding: 0.8rem 1.5rem;
        font-size: 1rem;
        text-transform: uppercase;
        letter-spacing: 1px;
        font-weight: 600;
        border-radius: 0.25rem;
        transition: all 0.3s ease;
        background-color: var(--primary);
        color: white;
        text-decoration: none;
    }

    .contact-btn:hover {
        background-color: var(--secondary);
        transform: translateY(-2px);
        box-shadow: 0 4px 12px rgba(255, 20, 147, 0.3);
    }

    @media (max-width: 768px) {
        .blog-contact-cta {
            padding: 1.5rem;
        }
    }
</style>